
<nav class="navbar navbar-light bg-light">
  <% if user_signed_in?%>
    Welcom, <%= current_user.name%>
    <div class="btn-group">
      <button type="button" class="btn btn-light"><%= project_link%></button>
        <button type="button" class="btn btn-light dropdown-toggle dropdown-toggle-split" data-bs-toggle="dropdown" aria-expanded="false">
      </button>
      <ul class="dropdown-menu">
        <li><a class="dropdown-item"><%= link_to 'Go out', destroy_user_session_path, method: :delete%></a></li>
        <%if current_user.admin?%>
          <li><a class="dropdown-item"><%= link_to 'Space Admin', admin_tests_path%></a></li>
        <%end%>
      </ul>
    </div>
  <%else%>
    <div class="d-grid gap-2 d-md-block">
      <button class="btn btn-light" type="button"><%= link_to "Log in", new_session_path(resource_name) %></button>
      <button class="btn btn-light" type="button"><%= link_to "Sign up", new_registration_path(resource_name) %></button>
    </div>
  <%end%>
</nav>
