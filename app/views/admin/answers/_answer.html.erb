<tr>
  <td><li><%= answer.body%><td>
  <td><%= link_to t('nav.show'), admin_answer_path(answer)%></td>
  <td><%= link_to t('nav.edit'), edit_admin_answer_path(answer)%></td>
  <td><%= link_to t('nav.delete'), admin_answer_path(answer),
                            method: :delete,
                            data: {confirm:  t('delete.message')}%></td>
</tr>
