<tr>
  <td><%= test.title%></td>
  <td><%= test.questions.count%></td>
  <td><%= link_to t('nav.show'), admin_test_path(test)%></td>
  <td><%= link_to t('nav.edit'), edit_admin_test_path(test)%></td>
  <td><%= link_to t('nav.delete'), admin_test_path(test),
                            method: :delete,
                            data: {confirm: t('delete.message')}%></td>
</tr>
