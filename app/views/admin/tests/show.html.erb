<p><b><%= t('.header', title: @test.title)%> :<%= link_to t('nav.edit'), edit_admin_test_path(@test)%></b></p>

<table class="table table-striped">
  <tr>
    <th><%= t('.questions')%></th>
    <th colspan = "3"><%= t('.action')%></th>
  </tr>

  <%= render @test.questions%>
</table>

<div class="d-grid gap-2 d-md-block">
  <button class="btn btn-light" type="button"><%= link_to t('nav.new_question'), new_admin_test_question_path(@test), class: "btn btn-light"%></button>
  <button class="btn btn-light" type="button"><%= link_to t('nav.back'), admin_tests_path, class: "btn btn-light"%></button>
</div><br>
