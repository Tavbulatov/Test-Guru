<p><b><%= t('.header', title: @test.title)%></b></p>
<table>
  <tr>
    <th><%= t('.questions')%></th>
    <th colspan = "3"><%= t('.action')%></th>
  </tr>

  <%= render @test.questions%>
</table>

<p><%= link_to t('nav.new_question'), new_admin_test_question_path(@test)%>

<p><%= link_to t('nav.back'), admin_tests_path%></p>
